# 前端环境变量示例

# === 标准开发 ===
# 默认 npm run dev 使用此文件，留空即可

# === UI-only 调试（无后端）===
# 复制本文件为 .env.dummy，然后运行 npm run dev:dummy
# VITE_USE_DUMMY_AUTH=true

# === 后端地址（如有需要覆盖）===
# VITE_API_BASE_URL=http://localhost:8000