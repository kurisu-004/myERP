# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ---------- 虚拟环境 ----------
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ---------- 测试 / 覆盖率 ----------
.pytest_cache/
.coverage
.coverage.*
.cache
coverage.xml
*.cover
.hypothesis/
.tox/
.nox/

# 类型检查 / 静态分析
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# ---------- 工具 / 编辑器 ----------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.sublime-project
*.sublime-workspace

# ---------- 日志 / 临时文件 ----------
*.log
logs/
*.pid
*.seed
*.pid.lock
local_settings.py

# ---------- 环境变量 / 密钥（务必忽略）----------
.env
.env.*
!.env.example
!.env.production.example
*.pem
*.key
secrets.yaml
secrets.json

# ---------- 数据库 / 迁移产物 ----------
*.db
*.sqlite
*.sqlite3
*.mdb
postgres_data/
mysql_data/
data/
docker-compose.override.yml

# ---------- Alembic ----------
# 保留 alembic/ 目录本身和 versions/，只忽略缓存
alembic/__pycache__/
alembic/versions/__pycache__/

# ---------- API 文档 / 客户端生成 ----------
docs/_build/
docs/.sphinx/
.openapi-generator/

# ---------- 模型 / 数据文件 ----------
*.pkl
*.joblib
*.h5
*.hdf5
*.parquet
*.csv
!data/.gitkeep

# ---------- 前端（如有） ----------
node_modules/
.next/
.nuxt/
dist/
.parcel-cache/

# ---------- 操作系统 ----------
.DS_Store
.AppleDouble
.LSOverride
Icon?
ehthumbs.db
Desktop.ini

# ---------- 项目特定 ----------
# FastAPI
htmlcov/

# 临时测试文件
/tmp/
test_*.tmp

.agent/
.claude/

# Git worktree 隔离目录（每个分支一个 worktree，避免污染主 checkout）
.worktrees/

# 2026-07-15：真实客户数据 / 临时 Excel（送货单 / 加工明细 / 招标项目）不进 git。
# 这些文件含业务敏感信息且会频繁变更；只保留在本地 docs/example/ 作
# 调试 / 演示用，不入版本控制。Excel 模板（如应标批量导入的 .xlsx 模板）
# 如需入 git 放在别的 docs/ 子目录。
docs/example/**